# @package _global_

defaults:
  - override /patch_config: stableaudio_ablate
  - override /patch_layers: stableaudio/none
  - override /patch_data: musiccaps/drums
  - override /patch_model: stableaudio_patch
